<Window x:Class="CRUD_Navigation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRUD_Navigation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border BorderThickness="0,0,2,0" BorderBrush="Black">
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="VADM" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Grid.Row="1" Content="View" Command="{Binding View}" Style="{StaticResource NavButton}"/>
            <Button Grid.Row="2" Content="Add" Command="{Binding Add}" Style="{StaticResource NavButton}"/>
            <Button Grid.Row="3" Content="Delete" Command="{Binding Delete}" Style="{StaticResource NavButton}"/>
            <Button Grid.Row="4" Content="Modify" Command="{Binding Modify}" Style="{StaticResource NavButton}"/>

        </Grid>
        </Border>
            <ContentPresenter Grid.Column="1"
             HorizontalAlignment="Stretch" 
             VerticalAlignment="Stretch"
             Content="{Binding CurrentViewModel}" />        
        
        
    </Grid>
</Window>
