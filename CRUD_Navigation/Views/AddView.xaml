<UserControl x:Class="CRUD_Navigation.Views.AddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CRUD_Navigation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid  Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Border BorderThickness="0,0,0,1" BorderBrush="Black" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Grid.Row="0" Text="Add an Element" FontSize="40" Margin="0,10,0,20"/>
        </Border>
        <Grid Grid.Row="1" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition  Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Name"/>
            <TextBox Grid.Row="1" Width="150" Margin="5" Text="{Binding Name,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
        </Grid>

        <Grid Grid.Row="2" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition  Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Type"/>
            <ComboBox Style="{StaticResource ModernComboBox}" Grid.Row="1" ItemsSource="{Binding Types}" 
          SelectedItem="{Binding SelectedType}"
          HorizontalAlignment="Stretch"
          
          Margin="5"/>
        </Grid>

        <Grid Grid.Row="3" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition  Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Description"  />
            <TextBox Grid.Row="1" Width="400" 
                     MaxLength="100" Height="100"
                     TextWrapping="Wrap"
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto" Margin="5" Text="{Binding Description,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>       
        </Grid>

        <Button Grid.Row="4" Content="Add" Margin="0,10,0,0" Command="{Binding Add}" Style="{StaticResource PrimaryButton}"/>
    </Grid>
</UserControl>
