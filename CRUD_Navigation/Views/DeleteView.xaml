<UserControl x:Class="CRUD_Navigation.Views.DeleteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CRUD_Navigation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Border BorderThickness="0,0,0,1" BorderBrush="Black" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Grid.Row="0" Text="Delete An Element" FontSize="40" Margin="0,10,0,20"/>
        </Border>
        <Grid Grid.Row="1" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition  Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Element ID"/>
            <TextBox Grid.Row="1" Width="150" Margin="5" Text="{Binding Id,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
        </Grid>

        <Button Grid.Row="2" Content="Delete" Margin="0,10,0,0" Command="{Binding Delete}" Style="{StaticResource PrimaryButton}"/>



    </Grid>
</UserControl>
